{
  "name": "rentoX",
  "version": "1.0.0",
  "description": "Vehicle rental marketplace",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend 2>/dev/null || echo 'Frontend build not configured yet'",
    "test": "echo 'Test scripts not yet configured'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}